baseURL = 'https://blog.cml3366.cn/'
languageCode = 'zh-cn'
title = '宇宙之光 × 古典之韵'
# Updated: 2025-10-31 - Force rebuild for CDN cache refresh
theme = 'blowfish'
relativeURLs = true

[menu]
  [[menu.main]]
    name = 'RSS'
    url = '/index.xml'
    weight = 5

[params]
  colorScheme = 'dark'
  defaultAppearance = 'dark'
  tagline = '宇宙之光 × 古典之韵'
  customCSS = ["/css/reader.css"]
  
  [params.homepage]
    layout = "background"
    showRecent = true
    showRecentItems = 10
    showMoreLink = true
    showMoreLinkDest = "/posts/"

  [params.share]
    # 自定义二维码服务（将 {url} 替换为真实链接）。示例为 qrserver 公共服务
    qrApi = "https://api.qrserver.com/v1/create-qr-code/?size=220x220&data={url}"
    # 可选：自有短链（仅拼接，不发起后端请求），将 {url} 替换为真实链接
    shortBase = ""
